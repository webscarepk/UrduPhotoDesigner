<?xml version="1.0" encoding="utf-8"?>
<!--
    A material swatch, built like a colour swatch.

    Same construction as layout_color_item: an outer round card that carries the selection
    ring, and an inner card holding the preview that insets when selected so the chip
    shrinks inside the ring. The only addition is the caption — Pearl and Dove are the same
    grey until they are named.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_marginEnd="8dp"
    android:layout_marginBottom="6dp"
    android:gravity="center_horizontal"
    android:orientation="vertical">

    <com.google.android.material.card.MaterialCardView
        android:id="@+id/cardOuter"
        android:layout_width="44dp"
        android:layout_height="44dp"
        app:cardBackgroundColor="@color/white"
        app:cardCornerRadius="22dp"
        app:cardElevation="0dp"
        app:strokeColor="@android:color/transparent"
        app:strokeWidth="0dp">

        <com.google.android.material.card.MaterialCardView
            android:id="@+id/cardInner"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_margin="0dp"
            app:cardCornerRadius="22dp"
            app:cardElevation="0dp"
            app:strokeWidth="0dp">

            <ImageView
                android:id="@+id/swatchView"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:background="@color/contrast"
                android:contentDescription="@null"
                android:scaleType="centerCrop" />

        </com.google.android.material.card.MaterialCardView>

    </com.google.android.material.card.MaterialCardView>

    <TextView
        android:id="@+id/swatchLabel"
        android:layout_width="44dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="3dp"
        android:ellipsize="end"
        android:fontFamily="@font/regular"
        android:gravity="center"
        android:maxLines="1"
        android:textColor="#5F6368"
        android:textSize="9sp"
        tools:text="Pearl" />

</LinearLayout>
