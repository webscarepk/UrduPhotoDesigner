<?xml version="1.0" encoding="utf-8"?>
<!--
  Plan card row for SubscriptionsAdapter.
  IDs preserved from the original item: saveBadge, mainCard, title, price, duration.
  Added: radio circle (id/radio) toggled alongside the border in updateSelection().

  Selection look (matches the reference image):
   - selected   → appColor border (stroke set in adapter) + filled radio
   - unselected → no border + empty radio
-->
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/subscriptionCard"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginHorizontal="2dp"
    android:layout_marginBottom="12dp">

    <!-- Discount badge, pinned to the card's top-right corner -->
    <TextView
        android:id="@+id/saveBadge"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginEnd="14dp"
        android:background="@drawable/ic_button_gradient_wrap"
        android:fontFamily="@font/medium"
        android:paddingHorizontal="10dp"
        android:paddingVertical="3dp"
        android:text="@string/save_25"
        android:textColor="@color/white"
        android:textSize="10sp"
        android:translationZ="2dp"
        app:layout_constraintTop_toTopOf="@id/mainCard"
        app:layout_constraintBottom_toTopOf="@id/mainCard"
        app:layout_constraintEnd_toEndOf="@id/mainCard" />

    <com.google.android.material.card.MaterialCardView
        android:id="@+id/mainCard"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp"
        app:cardBackgroundColor="@color/white"
        app:cardCornerRadius="14dp"
        app:cardElevation="0dp"
        app:strokeColor="@color/appColor"
        app:strokeWidth="0dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent">

        <androidx.constraintlayout.widget.ConstraintLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:paddingHorizontal="16dp"
            android:paddingVertical="16dp">

            <!-- Radio circle (left). Image swapped by updateSelection() in adapter. -->
            <ImageView
                android:id="@+id/radio"
                android:layout_width="22dp"
                android:layout_height="22dp"
                android:src="@drawable/ic_radio_unselected"
                app:layout_constraintTop_toTopOf="parent"
                app:layout_constraintBottom_toBottomOf="parent"
                app:layout_constraintStart_toStartOf="parent" />

            <TextView
                android:id="@+id/title"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_marginStart="14dp"
                android:fontFamily="@font/bold"
                android:text="@string/monthly"
                android:textColor="@color/black"
                android:textSize="15sp"
                app:layout_constraintStart_toEndOf="@id/radio"
                app:layout_constraintEnd_toStartOf="@id/price"
                app:layout_constraintTop_toTopOf="parent" />

            <TextView
                android:id="@+id/duration"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_marginStart="14dp"
                android:layout_marginTop="2dp"
                android:fontFamily="@font/regular"
                android:text="@string/per_month"
                android:textColor="@color/gray"
                android:textSize="12sp"
                app:layout_constraintStart_toEndOf="@id/radio"
                app:layout_constraintEnd_toStartOf="@id/price"
                app:layout_constraintTop_toBottomOf="@id/title" />

            <!-- Price block (right) -->
            <TextView
                android:id="@+id/price"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:fontFamily="@font/bold"
                android:text="@string/rs_399"
                android:textColor="@color/appColor"
                android:textSize="18sp"
                app:layout_constraintTop_toTopOf="parent"
                app:layout_constraintBottom_toBottomOf="parent"
                app:layout_constraintEnd_toEndOf="parent" />
        </androidx.constraintlayout.widget.ConstraintLayout>
    </com.google.android.material.card.MaterialCardView>
</androidx.constraintlayout.widget.ConstraintLayout>