<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:background="@drawable/panel_bg"
    tools:context=".ui.editor.panels.text.TextFragment">

    <androidx.constraintlayout.widget.Guideline
        android:id="@+id/startGuide"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintGuide_percent="0.04"
        android:orientation="vertical" />

    <androidx.constraintlayout.widget.Guideline
        android:id="@+id/endGuide"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintGuide_percent="0.96"
        android:orientation="vertical" />

    <!-- ═══ TOP ROW: Title (left) + Drag handle (center) + Close (right) ══════
         Title and Close are ALWAYS visible — collapsed and expanded.
         Close collapses the panel in both states.                           -->

    <TextView
        android:id="@+id/panelTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/fonts"
        android:fontFamily="@font/bold"
        android:textSize="16sp"
        android:textColor="@color/black"
        app:layout_constraintStart_toEndOf="@id/startGuide"
        app:layout_constraintTop_toBottomOf="@id/dragHandle" />

    <ImageView
        android:id="@+id/dragHandle"
        android:layout_width="@dimen/_100sdp"
        android:layout_height="wrap_content"
        android:paddingVertical="12dp"
        android:src="@drawable/drag_handle_bg"
        android:importantForAccessibility="no"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Always visible — collapses/expands the panel -->
    <ImageView
        android:id="@+id/closePanel"
        android:layout_width="28dp"
        android:layout_height="28dp"
        android:padding="6dp"
        android:background="@drawable/button_bg_small"
        android:backgroundTint="@color/contrast"
        app:tint="@color/black"
        android:src="@drawable/ic_close"
        app:layout_constraintEnd_toStartOf="@id/endGuide"
        app:layout_constraintTop_toTopOf="@id/panelTitle"
        app:layout_constraintBottom_toBottomOf="@id/panelTitle" />

    <!-- ═══ TOOLBAR ROW ════════════════════════════════════════════════════ -->
    <LinearLayout
        android:id="@+id/header"
        android:layout_width="0dp"
        android:layout_height="42dp"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        app:layout_constraintEnd_toStartOf="@id/endGuide"
        app:layout_constraintStart_toEndOf="@id/startGuide"
        app:layout_constraintTop_toBottomOf="@id/panelTitle">

        <ImageView
            android:id="@+id/addText"
            android:layout_height="30dp"
            android:layout_width="30dp"
            android:padding="8dp"
            android:background="@drawable/button_bg_small"
            android:backgroundTint="#30005D28"
            android:src="@drawable/ic_add_text" />

        <ImageView
            android:id="@+id/addFont"
            android:layout_height="30dp"
            android:layout_width="30dp"
            android:layout_marginStart="6dp"
            android:padding="8dp"
            android:background="@drawable/button_bg_small"
            android:backgroundTint="@color/contrast"
            android:src="@drawable/ic_add_font" />

        <!-- Collapsed only: tap to show inline search -->
        <ImageView
            android:id="@+id/searchIcon"
            android:layout_width="30dp"
            android:layout_height="30dp"
            android:layout_marginStart="6dp"
            android:padding="8dp"
            android:background="@drawable/button_bg_small"
            android:backgroundTint="@color/contrast"
            app:tint="@color/gray"
            android:src="@drawable/ic_search" />

        <com.google.android.material.tabs.TabLayout
            android:id="@+id/tabLayout"
            android:layout_width="0dp"
            android:layout_height="42dp"
            android:layout_weight="1"
            android:layout_marginStart="8dp"
            android:background="@android:color/transparent"
            app:tabIndicatorColor="@color/appColor"
            app:tabIndicatorHeight="1dp"
            app:tabPadding="0dp"
            app:tabMode="auto"
            app:tabGravity="fill"
            app:tabSelectedTextColor="@color/appColor"
            app:tabTextColor="@color/black"
            app:tabRippleColor="@color/contrast" />

    </LinearLayout>

    <!-- Search row — gone in collapsed, visible on Font tab when expanded -->
    <LinearLayout
        android:id="@+id/searchRow"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:visibility="gone"
        android:paddingVertical="4dp"
        app:layout_constraintTop_toBottomOf="@id/header"
        app:layout_constraintStart_toEndOf="@id/startGuide"
        app:layout_constraintEnd_toStartOf="@id/endGuide">

        <EditText
            android:id="@+id/searchBar"
            android:layout_width="0dp"
            android:layout_height="32dp"
            android:layout_weight="1"
            android:textColor="@color/black"
            android:fontFamily="@font/regular"
            android:textSize="12sp"
            android:maxLines="1"
            android:singleLine="true"
            android:background="@drawable/button_bg_small"
            android:paddingHorizontal="12dp"
            android:drawablePadding="6dp"
            android:textColorHint="@android:color/darker_gray"
            android:backgroundTint="@color/contrast"
            android:hint="@string/search_here" />

    </LinearLayout>

    <androidx.viewpager2.widget.ViewPager2
        android:id="@+id/viewPager"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_marginTop="4dp"
        app:layout_constraintTop_toBottomOf="@id/searchRow"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toEndOf="@id/startGuide"
        app:layout_constraintEnd_toStartOf="@id/endGuide" />

</androidx.constraintlayout.widget.ConstraintLayout>